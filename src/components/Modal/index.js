import styled from "styled-components";

export const Modal = styled.div`
  width: 500px;
  padding: 18px;
  border: 1px solid lightgray;
  border-radius: var(--main-borde-radius);
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
